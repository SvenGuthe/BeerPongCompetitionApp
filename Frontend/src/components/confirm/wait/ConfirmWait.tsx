import { Alert } from "react-bootstrap";

const ConfirmWait: React.FC = () => {
    return <Alert variant="info">Aktiviere deinen Account durch den Bestätigungslink in deinen Mails!</Alert>
};

export default ConfirmWait;